import React from 'react';
import ReactLoading from 'react-loading';

const Example = ({ bubbles }) => (
  <ReactLoading type="bubbles" color="#282dd1" height={467} width={275} />
);

export default Example;
